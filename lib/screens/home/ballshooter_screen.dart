import 'package:flutter/material.dart';

class BallShooterScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Ball Shooter'),
      ),
    );
  }
}
