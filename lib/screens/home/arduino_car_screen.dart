import 'package:flutter/material.dart';

class ArduinoCarScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Arduino Car'),
      ),
    );
  }
}
