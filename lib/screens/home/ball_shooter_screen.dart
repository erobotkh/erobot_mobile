import 'package:flutter/material.dart';

class BallShotterScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('ball shotter '),
      ),
    );
  }
}
